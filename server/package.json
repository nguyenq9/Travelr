{
    "name": "ravenous",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "bcrypt": "^5.1.0",
        "bcryptjs": "^2.4.3",
        "concurrently": "^7.6.0",
        "dotenv": "^16.0.3",
        "express": "^4.18.2",
        "http-proxy-middleware": "^2.0.6",
        "mongoose": "^6.10.2"
    },
    "scripts": {
        "server": "node server.js",
        "travelr": "npm run start --prefix ../travelr",
        "dev": "concurrently \"npm run server\" \"npm run travelr\""
    },
    "eslintConfig": {
        "extends": [
            "react-app",
            "react-app/jest"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "nodemon": "^2.0.20"
    }
}
